# an AVL tree is a self balancing binary search tree that maintains the height of the tree to be logarithmic
# O(log n) computation time for search, insert, and delete operations
# has a balanace factor
# BF(X) = height(left_subtree) - height(right_subtree)

# A node X